# Homebrew Tap

This directory contains the Homebrew formula for SpecLedger.

## Installation

```bash
brew tap specledger/specledger https://github.com/specledger/specledger.git
brew install specledger
```

## Formula

The formula file (`specledger.rb`) is automatically updated by GoReleaser during releases.
