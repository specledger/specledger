# Implementation Plan: New CLI Commands and Skills

**Branch**: `601-cli-skills` | **Date**: 2026-03-03 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specledger/601-cli-skills/spec.md`

## Summary

Add new CLI commands and skills for comment management, research spikes, and implementation checkpoints. This is **Stream 3** of the 3-stream SDD alignment effort, building on the architecture decisions from 598 (D4, D13, D14, D21).

**Key Deliverables**:
1. Extract `sl comment` CLI from `sl revise` for granular comment CRUD
2. Create `sl-comment` skill for agent guidance
3. Add `spike` AI command for exploratory research
4. Add `checkpoint` AI command for implementation verification

## Technical Context

**Language/Version**: Go 1.24.2
**Primary Dependencies**: Cobra (CLI), Supabase PostgREST (comments API), YAML v3 (config)
**Storage**: Supabase PostgREST (review_comments table), file-based (research files, session logs)
**Testing**: `go test` with table-driven tests, contract tests for API
**Target Platform**: Cross-platform (macOS, Linux, Windows)
**Project Type**: CLI tool (single binary) + AI commands (markdown files)
**Performance Goals**: All `sl comment` commands complete in <2s P95 (CLI execution time excluding network RTT to Supabase)
**Constraints**: Auth-required for comment API; no PTY requirement for CLI commands
**Scale/Scope**: ~6 new Go files, ~800 LOC new code, ~400 LOC extracted from revise package

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- [x] **Specification-First**: Spec.md complete with 6 prioritized user stories (P1/P2)
- [x] **Test-First**: Contract tests for PostgREST client, unit tests for CLI commands
- [x] **Code Quality**: golangci-lint with govet, staticcheck, errcheck, ineffassign, unused, gosec
- [x] **UX Consistency**: CLI patterns follow Data CRUD (D16), token-efficient output (D21)
- [x] **Performance**: <2s target for comment commands (network RTT dominates)
- [x] **Observability**: Errors to stderr, structured JSON output for machine consumption
- [x] **Issue Tracking**: Linked to 598 epic for architecture decisions

**Complexity Violations** (if any, justify in Complexity Tracking table below):
- None identified - follows established patterns from 136-revise-comments and 598

## Project Structure

### Documentation (this feature)

```text
specledger/601-cli-skills/
├── plan.md              # This file
├── research.md          # Extraction patterns, skill structure
├── spec.md              # Feature specification
├── checklists/          # Quality validation
│   └── requirements.md
├── contracts/           # CLI contracts
│   └── sl-comment-cli.md
└── tasks.md             # Generated by /specledger.tasks
```

### Source Code (repository root)

```text
pkg/cli/comment/                 # NEW — Extracted from revise/
├── types.go                     # ReviewComment, ThreadReply, output types
├── client.go                    # PostgREST client (moved from revise/)
└── client_test.go               # Unit tests for client operations

pkg/cli/commands/
├── comment.go                   # NEW — sl comment CLI (list/show/reply/resolve)
└── revise.go                    # MODIFIED — import from comment/ package

pkg/cli/revise/                  # MODIFIED — revise-specific logic only
├── types.go                     # MODIFIED — remove comment types
├── client.go                    # MODIFIED — remove comment client
├── prompt.go                    # Unchanged
├── editor.go                    # Unchanged
└── automation.go                # Unchanged

pkg/embedded/skills/
└── sl-comment/                  # NEW — sl-comment skill
    └── skill.md                 # When to use, decision criteria, JSON examples

.claude/commands/
├── specledger.spike.md          # NEW — spike AI command
├── specledger.checkpoint.md     # NEW — checkpoint AI command
└── specledger.clarify.md        # MODIFIED — use sl comment list
```

**Structure Decision**: Single project structure. New `pkg/cli/comment/` package extracted from `pkg/cli/revise/`. AI commands as markdown files in `.claude/commands/`.

## Complexity Tracking

> No violations identified - extraction follows established patterns.

## Phase 0: Research Summary

See [research.md](./research.md) for detailed analysis. Key findings:

### Existing Code to Extract (from 136-revise-comments)

| Source | Target | What to Extract |
|--------|--------|-----------------|
| `pkg/cli/revise/types.go` | `pkg/cli/comment/types.go` | `ReviewComment`, `ThreadReply`, `ReplyMap` |
| `pkg/cli/revise/client.go` | `pkg/cli/comment/client.go` | `FetchComments()`, `FetchReplies()`, `ResolveComment()`, `ResolveCommentWithReplies()` |
| `pkg/cli/revise/client.go` | `pkg/cli/comment/client.go` | PostgREST chain: `GetProject()` → `GetSpec()` → `GetChange()` → comments |

### New Code to Create

| Component | Type | Files |
|-----------|------|-------|
| `sl comment list` | CLI | `pkg/cli/commands/comment.go` |
| `sl comment show` | CLI | `pkg/cli/commands/comment.go` |
| `sl comment reply` | CLI | `pkg/cli/commands/comment.go` + `pkg/cli/comment/client.go` |
| `sl comment resolve` | CLI | `pkg/cli/commands/comment.go` |
| `sl-comment` skill | Skill | `pkg/embedded/skills/sl-comment/skill.md` |
| `/specledger.spike` | AI Command | `.claude/commands/specledger.spike.md` |
| `/specledger.checkpoint` | AI Command | `.claude/commands/specledger.checkpoint.md` |

### Dependencies on Previous Streams

| Stream | Dependency | Impact |
|--------|------------|--------|
| 599-alignment | `/specledger.clarify` update | Must use `sl comment list` instead of `sl revise --summary` |
| 600-bash-cli-migration | None | Independent |

## Phase 1: Design

### Data Model

**From existing 136-revise-comments** (no changes needed):

```go
// ReviewComment represents a review comment from Supabase
type ReviewComment struct {
    ID           string  `json:"id"`
    FilePath     string  `json:"file_path"`
    Line         *int    `json:"line"`
    StartLine    *int    `json:"start_line"`
    Content      string  `json:"content"`
    SelectedText string  `json:"selected_text"`
    AuthorName   string  `json:"author_name"`
    AuthorEmail  string  `json:"author_email"`
    Status       string  `json:"status"`
    CreatedAt    string  `json:"created_at"`
}

// ThreadReply represents a reply in a comment thread
type ThreadReply struct {
    ID         string `json:"id"`
    ParentID   string `json:"parent_id"`
    Content    string `json:"content"`
    AuthorName string `json:"author_name"`
    CreatedAt  string `json:"created_at"`
}
```

### CLI Contracts

See [contracts/sl-comment-cli.md](./contracts/sl-comment-cli.md) for full contract.

**Token-efficient output (D21)**:
- **Compact mode** (default for `sl comment list`):
  - Previews truncated to 80 chars with "..." ellipsis
  - Shows count instead of full data (e.g., "3 replies")
  - One comment per line in table format
  - Example: `abc123 | src/main.go:42 | Fix error handling... | alice | 3 replies`
- **Full mode** (`--json` flag or `sl comment show`):
  - Complete data with no truncation
  - All thread replies included
  - Structured JSON for programmatic parsing

### AI Command Templates

**spike**: Research spike
- Output: `specledger/<spec>/research/yyyy-mm-dd-<topic>.md`
- Sections: Findings, Decisions, Recommendations

**checkpoint**: Implementation verification
- Output: Session log update at `.specledger/sessions/<spec>-session.md`
- Actions: Verify tests (`go test ./...` for modified packages, exit 0), note uncommitted changes, summarize progress

## Phase 2: Work Breakdown

### US1: sl comment list Command (P1)

**Files**:
- `pkg/cli/comment/types.go` - Extract from revise
- `pkg/cli/comment/client.go` - Extract from revise
- `pkg/cli/commands/comment.go` - New list subcommand

**Tasks**:
1. Extract `ReviewComment`, `ThreadReply`, `ReplyMap` to `pkg/cli/comment/types.go`
2. Extract `FetchComments()`, PostgREST chain to `pkg/cli/comment/client.go`
3. Create `sl comment list` subcommand with `--json`, `--status` flags
4. Implement token-efficient compact output (D21)
5. Update `pkg/cli/revise/` imports

**Independent Test**: `sl comment list --json --status open` returns valid JSON

### US2: sl comment show Command (P1)

**Files**:
- `pkg/cli/commands/comment.go` - Add show subcommand

**Tasks**:
1. Create `sl comment show` subcommand accepting one or more IDs
2. Fetch full comment with thread replies
3. Implement `--json` output with full content
4. Handle "comment not found" error

**Independent Test**: `sl comment show <id> --json` returns full content

### US3: sl comment reply/resolve Commands (P1)

**Files**:
- `pkg/cli/comment/client.go` - Add `CreateReply()` method
- `pkg/cli/commands/comment.go` - Add reply, resolve subcommands

**Tasks**:
1. Add `CreateReply()` method to comment client (POST to PostgREST)
2. Create `sl comment reply <id> "message"` subcommand
3. Update `sl comment resolve` to use extracted client
4. Implement cascade resolve (parent → thread replies)

**Independent Test**: `sl comment reply <id> "message"` posts reply successfully

### US4: sl-comment Skill (P2)

**Files**:
- `pkg/embedded/skills/sl-comment/skill.md` - New skill

**Tasks**:
1. Create skill following `sl-issue-tracking` pattern
2. Document when to use each subcommand
3. Include JSON parsing examples
4. Document reply/resolve workflow

**Independent Test**: Skill loads and provides actionable guidance

### US5: spike AI Command (P2)

**Files**:
- `.claude/commands/specledger.spike.md` - New AI command

**Tasks**:
1. Create spike command template
2. Define output format (research file structure)
3. Include Findings/Decisions/Recommendations sections

**Independent Test**: `/specledger.spike "topic"` creates research file

### US6: checkpoint AI Command (P2)

**Files**:
- `.claude/commands/specledger.checkpoint.md` - New AI command

**Tasks**:
1. Create checkpoint command template
2. Define session log update logic
3. Add test verification steps
4. Include uncommitted changes handling

**Independent Test**: `/specledger.checkpoint` updates session log

## Dependencies

### Internal Dependencies
- **136-revise-comments**: Existing comment infrastructure (source for extraction)
- **599-alignment**: `/specledger.clarify` will use `sl comment list`

### External Dependencies
- `Supabase PostgREST`: Already in use for comments API
- `Cobra`: Already a dependency

## Risk Mitigation

| Risk | Mitigation |
|------|------------|
| Breaking existing `sl revise` | Keep revise working by importing from comment package |
| API rate limiting | Implement retry with backoff (already in client) |
| Skill not loading | Follow established skill patterns from sl-issue-tracking |

## Success Criteria

- [ ] SC-001: 4 new `sl comment` subcommands available
- [ ] SC-002: `sl comment list --json` produces valid JSON
- [ ] SC-003: `/specledger.clarify` uses `sl comment list`
- [ ] SC-004: `sl-comment` skill provides actionable guidance
- [ ] SC-005: `/specledger.spike` creates research files
- [ ] SC-006: `/specledger.checkpoint` tracks progress

## Next Steps

1. Run `/specledger.tasks` to generate detailed task breakdown
2. Implement US1-US3 (sl comment commands) first - P1 priority
3. Implement US4-US6 (skill and AI commands) - P2 priority
4. Update `/specledger.clarify` to use `sl comment list`
