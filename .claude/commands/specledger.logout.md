---
description: Logout from Specledger using the CLI.
---

## Execution

Khi user gọi `/specledger.logout`, chạy lệnh CLI:

```bash
sl auth logout
```

**Nếu `sl` chưa được cài đặt**, chạy:
```bash
go run ./cmd/sl auth logout
```

### Xác nhận

```bash
sl auth status
```
