{
  "$schema": "https://opencode.ai/schemas/opencode.json",
  "version": "1.0.0",
  "name": "SpecLedger Project",
  "description": "OpenCode configuration for SpecLedger project. Verify with: sl session list",
  "hooks": {
    "postToolUse": [
      {
        "matcher": "Bash",
        "command": "sl session capture"
      }
    ]
  }
}
