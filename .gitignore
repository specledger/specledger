# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html
coverage.txt

# Dependency directories
vendor/

# Go workspace file
go.work

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.log
*.bak
*.backup

# Beads local state (SQLite temp files)
.beads/beads.db-shm
.beads/beads.db-wal
.beads/.local_version
.beads/export-state/
.beads/issues.jsonl

# SpecLedger specific
.spec-cache/
specledger/vendor/
.spec-config/auth.json
.cache/
issues.jsonl.lock
.issues.jsonl.lock

# SpecLedger CLI credentials (FR-014: never commit tokens)
.specledger/credentials.json
**/credentials.json

# Personal-local config (gitignored per-user overrides)
specledger.local.yaml
**/specledger.local.yaml

# Claude Code local settings (per-user overrides, never commit)
.claude/settings.local.json

# Migration logs
.migration-log

# Build outputs
dist/
build/
*.tar.gz
*.zip

# Environment
.env
.env.*
!.env.example

# Database
*.db
*.sqlite
*.sqlite3

# Ledger files (if any)
ledger.db
*.ledger

bin/
bin/main
main
/sl
.beads/metadata.json
branch-map.json
tmp/
