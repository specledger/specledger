version: "1.0"
templates:
  - name: speckit
    description: "Spec Kit playbook templates with Claude Code commands, skills, and bash scripts"
    framework: "speckit"
    version: "1.0.0"
    path: "specledger"
    patterns:
      - "specledger/**"
      - ".claude/**"
      - ".beads/**"
